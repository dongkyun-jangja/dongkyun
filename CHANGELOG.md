# 변경 이력

## [미커밋 변경사항] - 2026-06-14

### 추가

- `claude/calculator.html` — 단일 파일 독립형 웹 계산기 애플리케이션 초기 구현
  - 기본 사칙연산 지원 (`+`, `−`, `×`, `÷`), 0 나눗셈 시 `Error` 표시
  - 연산자 연속 입력 시 자동 계산(체이닝) 처리
  - 부호 전환(`+/−`), 퍼센트(`%`), 소수점(`.`) 기능
  - 숫자 문자열이 10자를 초과하면 지수 표기(`toExponential(4)`)로 자동 전환
  - 연산 이력을 화면 상단 expression 영역에 실시간 표시
- 키보드 입력 지원 — 숫자, `+`, `-`, `*`, `/`(브라우저 검색 방지), `Enter`/`=`, `Escape`, `Backspace`
- macOS 스타일 윈도우 크롬(빨강·노랑·초록 도트 헤더)과 다크 테마(`#111111` 배경) 적용
- CSS Grid 4열 레이아웃 — 0 버튼은 `.span2`로 2열 점유
- `CLAUDE.md` — Claude Code용 프로젝트 구조·아키텍처 가이드 문서 추가
- `.claude/commands/changelog.md` — `/changelog` 커스텀 슬래시 커맨드 추가
